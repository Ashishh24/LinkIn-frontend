<div>
            <Header />
            {requests && <div>
                {requestsSent.map((request) => {
                    const {_id} = request;
                    const toUserID = request.toUserID;
                    const { firstName, lastName, bio, profilePhoto} = toUserID;

                    return (
                        <div key={_id} className="flex bg-base-200 border border-[#ccc] w-[50%] align-middle text-center p-5 mx-auto mt-5">
                            {/*profile photo*/}
                            <div className="w-[15%]">
                                <img className="w-20 rounded-full border-black" src={profilePhoto} />
                            </div> 
                            <div className="flex w-[90%] items-center">
                                <div className="w-[90%]">
                                    <div 
                                        className="flex space-x-2 text-xl cursor-pointer" 
                                        onClick={() => {
                                                navigate(`/profile/${toUserID._id}`, {
                                                // state: { _id, fromUserID }
                                            })}
                                        }
                                        >
                                            {firstName + " " + lastName}
                                    </div>
                                    <div className="text-left">
                                        {bio}
                                    </div>
                                </div>
                                {/* <div>
                                    <div className="flex gap-4">
                                        <button onClick={() => handleConnect(_id,"reject")} className="border rounded-3xl border-[#ccc] py-2 px-7 cursor-pointer hover:bg-red-500 hover:text-white">Reject</button>
                                        <button onClick={() => handleConnect(_id,"accept")} className="border rounded-3xl border-[#ccc] py-2 px-7 cursor-pointer hover:bg-green-500 hover:text-white">Accept</button>
                                    </div>
                                </div> */}
                            </div>
                        </div>
                    )
                })}
            </div>}
        </div>